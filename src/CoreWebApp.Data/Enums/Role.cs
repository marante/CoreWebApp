namespace CoreWebApp.Data
{
    /// <summary>
    /// Enum that defines different roles an employee can have within the system.
    /// </summary>
    public enum Role
    {
        CEO,
        CTO,
        CFO,
        Carpenter,
        Painter,
        TempWorker,
        ProjectManager
    }
}